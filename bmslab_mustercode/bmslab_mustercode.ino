#include <stdio.h>

void addition_of_two_lengths(int a_length1_m, int b_length2_m)
{
    //function for adding two lengths
    //programmer: Max Mustermann
    //e-mail: maxmustermann@hs-esslingen.de

    int sum;

    sum = a_length1_m + b_length2_m; //calculation

    return summe;
}

void setup()
{
    //main program for ...
    //programmer: Max Mustermann
    //e-mail: maxmustermann@hs-esslingen.de

    int k; //counting variable
    int a_length1_m = 4;
    int b_length2_m = 5;

    if (a_length1_m < b_length2_m) //Query whether length 1 is greater than length 2
    {
        addition_of_two_lengths(a_length1_m, b_length2_m);
    }

    for (k=0; k<=10; k++) //explanation...
    {
        /* code */
    }

    while (k < a_length1_m) //explanation...
    {
        /* code */
    }

    do //explanation...
    {
        /* code */
    } while (/* condition */);
    
return 0;
}


void loop() {
  // put your main code here, to run repeatedly:

}
